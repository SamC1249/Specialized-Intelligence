# plan.md — Daily Log

One or two lines per agent per day. Newest entries on top. See
`docs/plan-YYYY-MM-DD.md` for the full daily plans.

- [Adversarial-Agent @ 2026-07-12T17:03:00Z] Published `docs/plan-2026-07-12.md` attacking five concrete weaknesses in the seed pipeline (regex-fragile per-adapter license classifiers, IA `media_url` fabrication, static PeerTube allowlist, non-existent Common Crawl `search`, no dedup, uncalibrated quality weights, CI that doesn't enforce comparison-first), landed 3 research artifacts under `docs/artifacts/` (V-JEPA 2 curation, YFCC100M CC videos, perceptual video hashing), shipped H3 (shared conservative `license_utils.classify` + 33 adversarial tests: NC/ND anywhere in any candidate wins → RESTRICTED, all four adapters refactored to use it), H2 (metadata-only dedup by shingles + duration-bucket + author-slug with union-find clustering + new `BenchmarkResult.n_unique_after_dedup` and cross-source e2e test injecting a mirror), H4 (benchmark-guard PR workflow, mypy pre-commit, 80% coverage floor, precommit-compare smoke hook, 10 adversarial parser tests), and regenerated `reports/baseline-2026-07-12.json`. Suite is 71 tests / 87.5% coverage / 0 lint errors.
- [Coding-Agent @ 2026-06-20T17:12:32Z] Bootstrapped repo: `AGENTS.md`, canonical `db_structured.md` schemas, four offline source adapters (Wikimedia Commons, Internet Archive, PeerTube, Common Crawl recipe JSON-LD), metadata-only quality scorer, comparison harness + CLI, fixture-driven e2e test, GitHub Actions CI matrix on 3.11/3.12, and pre-commit. First baseline `reports/baseline-2026-06-20.json` ranks sources by mean quality: wikimedia 0.62 > peertube 0.57 > common_crawl 0.53 > archive_org 0.36 — future PRs must beat this on the same fixtures.

<!-- new entries above this line -->
