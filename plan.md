# plan.md — Daily Log

One or two lines per agent per day. Newest entries on top. See
`docs/plan-YYYY-MM-DD.md` for the full daily plans.

- [Coding-Agent @ 2026-07-16T17:35:00Z] Consolidated the two unmerged 2026-07-14 adversarial plans into `docs/plan-2026-07-16.md` and shipped the P0/P1 items: fixed `python -m specint compare --fixtures` (H5 — was returning all-zero reports), stamped real extractor SHA into every `Provenance` via `specint._version.get_extractor_git()` (H6), added within-source `n_duplicates` collapsing + `scorer` field on `BenchmarkResult`, introduced a named quality-scorer registry (`v1`, `v2`) with `text_density_min` gate + procedural-verb + english-signal components, added `run_matrix` + `SourceQuerySuite`, cross-source `dedup_records` helper, `python -m specint diff` / `matrix` subcommands, Common Crawl page-license extraction (double-evidence CC-BY / CC-BY-SA lift), robust license-classifier (NBSP + unseen CC-NC/ND/GFDL variants now correctly RESTRICTED/CC-BY-SA), new fixtures (multilingual wikimedia, restricted peertube, cc-licensed cookbook), 66 new offline tests (84 total), tightened CI (strict mypy on `records.py`+`quality/`, coverage floor ≥85%, sources smoke, v1/v2 e2e, matrix e2e, diff-vs-baseline), and generated `reports/{compare,compare-*-v2,matrix,scorer-compare,diff-2026-06-20-vs}-2026-07-16.json`. v2 scorer correctly demotes low-metadata Wikimedia records (n_license_clean unchanged, mean_quality -0.228 for wikimedia) — this is the informative signal H2 predicted.
- [Coding-Agent @ 2026-06-20T17:12:32Z] Bootstrapped repo: `AGENTS.md`, canonical `db_structured.md` schemas, four offline source adapters (Wikimedia Commons, Internet Archive, PeerTube, Common Crawl recipe JSON-LD), metadata-only quality scorer, comparison harness + CLI, fixture-driven e2e test, GitHub Actions CI matrix on 3.11/3.12, and pre-commit. First baseline `reports/baseline-2026-06-20.json` ranks sources by mean quality: wikimedia 0.62 > peertube 0.57 > common_crawl 0.53 > archive_org 0.36 — future PRs must beat this on the same fixtures.

<!-- new entries above this line -->
